# Static CDN vhost. Root is the host directory already used by Caddy; do not
# change files under /var/www/cdn.qrqto.club during the gateway cutover.

server {
    listen 80;
    listen [::]:80;
    server_name cdn.qrqto.club;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name cdn.qrqto.club;

    ssl_certificate     /etc/nginx/certs/cdn.qrqto.club/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/cdn.qrqto.club/privkey.pem;
    include /etc/nginx/snippets/love-book-ssl.conf;

    root /var/www/cdn.qrqto.club;
    index index.html;
    client_max_body_size 25m;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ =404;
        add_header Cache-Control "public, max-age=300";
        add_header Access-Control-Allow-Origin "*";
        add_header X-Content-Type-Options nosniff;
    }

    location ~* \.(php|jsp|asp|aspx|cgi)$ {
        deny all;
    }
}

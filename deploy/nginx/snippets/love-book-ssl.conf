# Host nginx TLS defaults for Love Book and CDN vhosts.
ssl_session_timeout 1d;
ssl_session_cache shared:LoveBookSSL:10m;
ssl_session_tickets off;
ssl_protocols TLSv1.2 TLSv1.3;
ssl_prefer_server_ciphers off;
